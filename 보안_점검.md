# 보안 점검 및 조치 가이드

> 이 시스템은 화면(브라우저)에서 직접 Supabase 데이터베이스에 접속하는 구조입니다.
> 그래서 **보안의 핵심은 두 가지** — ① 데이터베이스의 RLS(행 수준 보안), ② Supabase 인증(Auth) 설정 — 에 있습니다.

---

## 먼저 알아둘 점 (오해 방지)

- `app.js`·`login.html` 안에 보이는 **`SUPABASE_ANON_KEY`(공개 키)는 원래 외부에 공개되는 키**입니다. 노출되어도 그 자체로는 문제가 아닙니다.
- **진짜 방어선은 RLS**입니다. RLS가 제대로 걸려 있으면, 누가 이 공개 키로 데이터베이스를 직접 호출해도 **허락된 자기 데이터 외에는 보거나 바꿀 수 없습니다.**
- 반대로 RLS가 빠진 테이블이 하나라도 있으면, 그 테이블은 사실상 누구나 열람·수정할 수 있습니다. → 그래서 이번에 전체 테이블 RLS를 정비했습니다.
- AI용 진짜 비밀키(`ANTHROPIC_API_KEY`)는 서버(Edge Function)에만 있고 화면에 노출되지 않습니다. ✅ 안전.

---

## ✅ 이번에 코드로 이미 조치한 것 (배포만 하면 적용)

1. **저장형 XSS 차단** — 리스크명·현황·비고에 악성 코드(예: `<script>`)를 넣어도 그대로 실행되지 않고 글자로만 표시되도록 `app.js`의 출력부를 모두 이스케이프 처리.
2. **비밀번호 규칙 강화** — 회원가입 시 **8자 이상 + 영문·숫자 포함**을 요구(`login.html`).

> 배포: 평소처럼 `index.html` · `app.js` · `login.html`을 함께 올리면 됩니다.

---

## 🔴 1순위 — 데이터 유출 차단: RLS 적용 (꼭 실행)

`migration_2026-06-01_security_rls.sql` 파일을 실행합니다.

1. Supabase Dashboard → 왼쪽 **SQL Editor** 열기
2. `migration_2026-06-01_security_rls.sql` **파일 전체**를 복사해 붙여넣기
3. **[Run]** 클릭 (여러 번 실행해도 안전합니다)
4. 파일 아래쪽 **9번 "확인용 쿼리"** 의 `--` 를 지우고 하나씩 실행해 결과 확인
   - (a) 관리자 계정의 `approved` 가 **true** 인지
   - (b) 모든 테이블의 `rowsecurity` 가 **true** 인지
   - (c) 테이블마다 정책이 만들어졌는지

> ⚠️ 만약 관리자 계정 `approved` 가 false 이면, 관리자 본인도 데이터가 안 보일 수 있습니다.
> 그때는 SQL Editor에서:
> `update public.profiles set approved = true where email = 'gabeenya@gmail.com';`

### 적용 후 반드시 테스트
- 관리자로 로그인 → 대시보드·리스트·입력·회원관리가 정상 동작하는지
- 일반(승인) 회원으로 로그인 → 데이터가 보이고 입력되는지
- (가능하면) 승인 안 된 계정으로는 데이터가 안 보이는지

---

## 🟠 2순위 — Supabase 인증(Auth) 설정 강화 (대시보드에서 클릭)

Supabase Dashboard → **Authentication → Settings(또는 Policies/Providers)** 에서:

1. **Minimum password length** → **8** 이상으로 설정
   (코드 쪽은 이미 8자로 맞췄고, 서버 설정도 같이 올려야 완전합니다.)
2. **Leaked password protection**(유출된 비밀번호 차단, HaveIBeenPwned) → **켜기**
   - 과거 유출 이력이 있는 비밀번호는 가입·변경 시 거부됩니다.
3. **Site URL / Redirect URLs** → **실제 사용하는 주소만** 등록
   - 알 수 없는 주소로의 인증 우회를 막습니다.
4. **이메일 인증(Confirm email)** — 켜면 보안은 좋아지지만 ⚠️ 주의:
   - 현재 가입 흐름은 "가입 직후 곧바로 프로필 저장"을 전제로 합니다.
   - 이메일 인증을 켜면 인증 완료 전에는 세션이 없어 **프로필 저장이 실패**할 수 있습니다.
   - 켜고 싶으시면 먼저 말씀해 주세요. 가입 흐름을 그에 맞게 수정해 드리겠습니다.

---

## 🟡 3순위 — 운영·배포 보안 (권장)

1. **HTTPS(자물쇠) 사용 확인** — GitHub Pages / Netlify 등은 기본 HTTPS. 평문(http) 주소로는 쓰지 않기.
2. **관리자 계정 보호**
   - 관리자(가비님) 비밀번호는 길고 고유하게, 가능하면 별도 보관(비밀번호 관리자).
   - 관리자 이메일 계정 자체에 2단계 인증(2FA)을 걸어두면 더 안전.
3. **Mock 데이터 도구 정리** — 데이터 입력 화면 하단의 "Mock 데이터 도구(임시)"는 검토용입니다.
   실제 운영 전에는 숨기거나 제거하는 것을 권장합니다. (원하시면 처리해 드립니다.)
4. **회원 관리 주기적 점검** — 승인 대기 목록을 주기적으로 확인하고, 모르는 신청은 **삭제**.
   퇴사·미사용 계정은 **승인 해제**.
5. **Edge Function 점검** — `analyze-risk` 함수가 요청자의 로그인 토큰(JWT)과 승인 여부를 검증하는지 확인(현재 그렇게 구성되어 있음). API 키는 Supabase Secrets에만 두기.

---

## 점검 체크리스트 (요약)

- [ ] `migration_2026-06-01_security_rls.sql` 실행 완료
- [ ] 확인 쿼리로 RLS·정책·관리자 승인 상태 점검
- [ ] 관리자/일반 회원 로그인 테스트 통과
- [ ] Auth: 최소 비밀번호 길이 8 / 유출 비밀번호 차단 켜기
- [ ] Auth: Site URL·Redirect URL을 실제 주소로 제한
- [ ] 변경된 `app.js`·`login.html` 배포
- [ ] (선택) Mock 데이터 도구 제거, 이메일 인증 도입 검토

---

*이 문서는 보안 조치 기록용입니다. 설정을 바꾼 뒤에는 날짜와 함께 메모해 두세요.*
